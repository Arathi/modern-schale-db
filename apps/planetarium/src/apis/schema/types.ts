export enum Server {
  Japan = 'japan',
  Global = 'global',
  China = 'china',
}

export enum Language {
  Japanese = 'jp',
  English = 'en',
  Chinese = 'zh',
}

export type Released = [boolean, boolean, boolean];
